# Core deep learning stack
# Install the torch/torchvision build that matches your CUDA version from:
# https://pytorch.org/get-started/locally/
torch>=2.4
torchvision>=0.19
torchmetrics>=1.4

# Data and image processing
Pillow>=10.0
numpy>=1.26
scipy>=1.11
pandas>=2.0
PyYAML>=6.0

# DINO / distillation utilities
omegaconf>=2.3
ftfy>=6.1
regex>=2023.0
scikit-learn>=1.3
submitit>=1.5
termcolor>=2.3
packaging>=23.0

# Monitoring / optional visual reports
matplotlib>=3.8

# Optional acceleration used by vendor/dinov3 when available.
# Install separately only if your CUDA/PyTorch environment supports it:
# xformers
